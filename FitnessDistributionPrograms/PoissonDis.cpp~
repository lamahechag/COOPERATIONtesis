// Proceso de Moran
#include <iostream> 
#include<string>
#include<fstream>
#include<iomanip>
#include <ctime> 
#include <cstdlib> 
#include<cmath>
#include "Random.h"

using namespace std; 
int main () 
{
  double noise=1, k=2, b,c,a=2;
  c=noise/k; b=k*k/noise;
Random rv;
 for ( int i = 0; i < 1000; i++ ) cout << rv.gamma(a,b,c)
<< endl;
 return (0);
}
